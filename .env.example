# Warehouse credentials — only used by docker compose locally.
WAREHOUSE_USER=olist
WAREHOUSE_PASSWORD=olist_local_dev
WAREHOUSE_DB=olist

# Match your local UID so Airflow logs are owned by you on the host.
AIRFLOW_UID=50000
